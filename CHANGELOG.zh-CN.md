# 更新记录

[English](./CHANGELOG.md)

本项目仍处于早期阶段。发布版本后，从最新版本开始按日期记录用户可见变化。

## Unreleased

### Added

- 本地生成配对二维码，贯通授权、命名和邀请连接；首次使用并发标签页保持同一设备密钥
- 输入框支持粘贴文字、截图与多个文件，附件确认、移除后进入有上限的顺序发送队列
- 增加脱敏的 WSS、SDP 操作、ICE 候选/状态与重启 Console 日志，附带连接轮次并保留最近 200 条内存记录
- 开源许可证、贡献指南、安全策略和部署文档
- 独立 coturn Compose 与安全配置示例
- 创建码按需 Turnstile 验证
- REST、设备和 WebSocket 的分层限流
- CI、依赖更新和密钥扫描配置
- 必需的存储授权门禁与首次使用设备命名
- 直连在线时展示对方 IP

### Changed

- 移除自动独立 STUN 探测，新客户端恢复登记合入 WSS；双方确认后提前释放信令，保留原有 30 秒兜底窗口
- 增加全局 Socket 上限；15 秒一次的 Node 健康检查改为 30 秒一次的轻量检查，并关闭健康请求常规日志
- 离线输入区改为手动重试连接，取消进入会话、掉线和联网后的自动重连与遮罩
- 点击重试先复用短暂保留的已认证路径，失败再走新 ICE；恢复角色在设备证明后自动分配，并隔离不同协商轮次的旧信令，不依赖双方先后顺序
- 复查加固：Host 验证后再替换、限制待验证 Guest 时长、保护已获准对端并隔离旧批准结果、合并与取消地址探测，删除旧重连和房间覆盖分支
- 隔离 ICE 候选代次、限制恢复总时长，并完整结束掉线文件状态
- 可加入房间立即销毁，已认证信令保留最多 30 秒稳定窗口；允许已验证身份跨出口 IP 连接

- 已知设备离线后恢复时创建新的已认证会合
- 移除自定义对端 IP 路由及历史保存值
- 缩短左栏连接按钮文案并增加 GitHub 仓库入口

### Security

- 升级 Fastify 及有安全告警的运行和构建间接依赖，发布验证时 npm audit 无漏洞
- 公共健康检查不再返回房间数量
- TURN 地址与凭据不进入默认配置、公开 API 或前端构建
- 创建与恢复接口增加同源检查
